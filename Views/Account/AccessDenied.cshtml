@{
    ViewData["Title"] = "Acceso Denegado";
}

<section class="section" style="text-align:center;min-height:60vh;display:flex;align-items:center">
    <div class="container">
        <h1 style="font-family:var(--font-display);font-size:var(--fs-2xl);margin-bottom:16px">Acceso Denegado</h1>
        <p style="color:var(--ink-60);margin-bottom:32px">No tenés permisos para acceder a esta página.</p>
        <a href="/" class="btn btn-outline">← Volver al inicio</a>
    </div>
</section>
